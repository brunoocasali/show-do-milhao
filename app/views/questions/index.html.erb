<div class="col-md-12 col-md-offset-0">
  <div class="portlet">
    <div class="page-header">
      <h2>Questões!</h2>
      <div class="text-right">
        <%= link_to raw('<span class="glyphicon glyphicon-plus"></span> Contrato!'),
                    new_question_path, class: 'btn btn-primary' %>
      </div>
    </div>
    <div class="portlet-content row">
      <div class="col-md-12">
        <div class="table-responsive">
          <table class="table table-hover table-condensed">
            <thead>
            <tr>
              <th>Descrição</th>
              <th>Resposta Certa!</th>
              <th>Assunto</th>
              <th colspan="2">Opções</th>
            </tr>
            </thead>

            <tbody>
            <% @questions.each do |question| %>
                <tr>
                  <td><%= question.description %></td>
                  <td><%= question.correct_answer.title unless question.correct_answer.blank? %></td>
                  <td><%= question.subject.title %></td>
                  <td><%= link_to 'Show', question, class: 'btn btn-primary center-block' %></td>
                  <td><%= link_to 'Edit', edit_question_path(question), class: 'btn btn-primary center-block' %></td>
                  <td><%= link_to 'Destroy', question, method: :delete,
                                  data: { confirm: 'Are you sure?' }, class: 'btn btn-primary center-block' %></td>
                </tr>
            <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>